<%= form_for(match) do |f| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  </div>
  <div class="field">
    <%= f.label :tournament_name %>
    <%= f.select :tournament_name , Tournament.all.collect {|tour| [tour.id,tour.name]}%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
