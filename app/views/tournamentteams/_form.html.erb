<%= form_for(tournamentteam) do |f| %>
  <% if tournamentteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tournamentteam.errors.count, "error") %> prohibited this tournamentteam from being saved:</h2>

      <ul>
      <% tournamentteam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :team_id%>
    <%= f.select :team_id, Team.all.collect {|x| [x.name, x.id]}, {:include_blank => "Select One"} %> </br>
  </div>
  <div class="field">
    <%= f.label :tournament_id%>
    <%= f.select :tournament_id, Tournament.all.collect {|x| [x.name, x.id]}, {:include_blank => "Select One"} %> </br>
  </div>
  <div class="actions">
    <%= f.submit %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
